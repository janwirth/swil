SPEC: generation update
|> birdie snapshots
|> Pipe through gleam format
|> copy whole source file over
|> problem is: changes to the schema will block generation (oh that's just the current codebase) - so not if it's used a s a tool
